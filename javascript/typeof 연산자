typeof 연산자
typeof는 변수의 데이터 타입을 반환하는 연산자입니다.
자바스크립트는 변수 타입을 문자로 반환하는 함수인 typeof 연산자를 제공합니다.
typeof 연산자는 undefined, null, boolean, number, string, symbol, object, function의 자바스크립트가 가진 7가지 변수 타입을 구분하는 용도로 사용합니다.
이를 쓰는 이유는 기본적인 선언을 var로 퉁쳐서 해버리기 때문입니다.
구분을 하는데 있어 typeof 가 쓰이기도 하는 것입니다.

문법
typeof variable
variable에는 데이터 또는 변수가 들어갑니다. 괄호를 사용해도 됩니다.

typeof(variable)
반환되는 값은 다음과 같습니다.

undefined : 변수가 정의되지 않거나 값이 없을 때
number : 데이터 타입이 수일 때
string : 데이터 타입이 문자열일 때
boolean : 데이터 타입이 불리언일 때
object : 데이터 타입이 함수, 배열 등 객체일 때
function : 변수의 값이 함수일 때
symbol : 데이터 타입이 심볼일 때

